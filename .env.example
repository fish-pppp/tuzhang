# 复制成 .env 后按需修改：cp .env.example .env
# Vite 会自动读取 .env（服务端代码用 process.env，浏览器只能读 VITE_ 前缀）。

# ── 数据库 ─────────────────────────────────────────────────────────────────────
# 本地开发（推荐）：docker compose up -d 起一个 Postgres，然后用下面这条。
# 留空则退回内存版 PGLite：能跑，但重启 dev server 后账号和群组全部清空。
# 线上（Vercel）必填，推荐 Neon 的 pooled 连接串。
DATABASE_URL=postgres://tuzhang:tuzhang@127.0.0.1:5432/tuzhang

# ── 登录 ───────────────────────────────────────────────────────────────────────
# 会话签名密钥。本地随便写一串即可；线上必填且不要泄露：openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-to-a-long-random-string

# 站点访问地址（不带末尾斜杠）。浏览器地址栏的 origin 必须和它一致，否则登录报 Invalid origin。
# 本地：http://localhost:8080 ；线上：https://你的域名（Vercel 上不填会自动用部署域名）。
BETTER_AUTH_URL=http://localhost:8080

# 额外信任的登录来源（逗号分隔），例如同时开了 www 和裸域：https://www.example.com,https://example.com
# BETTER_AUTH_TRUSTED_ORIGINS=

# 自建（Docker）时设为 1：生产环境会强制要求 BETTER_AUTH_SECRET，启动时跑迁移。
# TUZHANG_SELF_HOST=1

# 默认开启登录。只有本地不想登录调试时才设成 false（此时不能同时配 DATABASE_URL）。
# VITE_AUTH_ENABLED=true

# 是否在登录页显示 Google / X。国内自建建议 false（这两个站点在大陆通常打不开）。
# 构建期生效。不填则显示。Docker 生产镜像默认 false。
# VITE_SHOW_OAUTH=false

# 可选：Grok 登录中转（Google / X）。自己部署一般没有这些密钥，直接用邮箱注册即可。
# GROK_AUTH_ISSUER=https://auth.grok.me
# GROK_AUTH_CLIENT_ID=
# GROK_AUTH_CLIENT_SECRET=
